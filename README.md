# Money Graph — объяснимый анализ транзакционной сети

Инструмент для AML-аналитика банка: по обезличенным переводам назначает каждому узлу структурную роль, выделяет сообщества и формирует очередь проверки с числовыми обоснованиями. В браузере можно найти полный `gid`, увидеть прямых плательщиков и получателей, суммы, количество переводов, правило роли и состав приоритета.

Результаты — гипотезы для проверки по наблюдаемому графу. Имена, возраст, доход и другие отсутствующие атрибуты клиента не добавляются. `role_score` показывает заданную силу правила, а не вероятность правонарушения.

Инструкция и сценарий выступления: [HOWTO.md](HOWTO.md). Результаты аудита: [AUDIT_REPORT.md](AUDIT_REPORT.md). Безопасность: [SECURITY.md](SECURITY.md).

## Запуск одной командой

Нужен **Python 3.10 или новее**. Для предоставленных Markdown-таблиц достаточно стандартной библиотеки. Команды выполняются в каталоге проекта, на этой машине — `C:\hackalemai`.

```powershell
python pipeline.py
```

До запуска в `data/` должны находиться `nodes.md`, `edges.md`, `transactions.md`, полученные от организаторов. Данные исключены из Git: в чистую копию исходников их нужно передать отдельно по разрешенному командой каналу. После подготовки входов команда выше создает все результаты без промежуточных ручных действий; каталог `out/` создается автоматически.

Успешный запуск возвращает код **0**, печатает число строк, распределение ролей, пороги, пять первых кандидатов и пути результатов. Ожидаются **2 248 узлов, 3 119 направленных пар, 4 840 транзакций и 91 кластер**.

| Результат | Состав |
|---|---|
| `out/nodes_roles.csv` | Все узлы: `gid, role, role_score, cluster_id, priority_score, evidence` |
| `out/clusters.csv` | Все сообщества: `cluster_id, n_nodes, n_seed, sum_kzt_internal, top_gids, hypothesis` |
| `out/top_nodes.csv` | Очередь проверки: `rank, gid, role, priority_score, why`; по умолчанию 100 строк |
| `out/index.html` | Самодостаточный интерфейс со встроенным графом |
| `out/analysis_summary.json` | Числа проверки данных, фактические пороги, роли и время расчета |

Пути и размер очереди задаются явно:

```powershell
python pipeline.py --data data --out out --top 100
```

`--top` ограничивается снизу значением 20; если всего узлов меньше 20, выводятся все имеющиеся. Контракт хакатона проверяется на полном наборе из 2 248 узлов.

## Вход из Parquet

Для Parquet нужна одна прямая зависимость: **`pyarrow==25.0.1`**, закрепленная в `requirements.txt`. Однократная подготовка окружения:

```powershell
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
```

Поместите разрешенные `nodes.parquet`, `edges.parquet`, `transactions.parquet` в `parquet_data/`. Полный пересчет выполняется одной командой:

```powershell
.venv\Scripts\python.exe pipeline.py --data parquet_data --out parquet_out
```

Загрузчик предпочитает полный набор Markdown, если в каталоге есть все три `.md`; иначе использует полный набор Parquet. Смешанный набор форматов не объединяется. Отдельный каталог устраняет неоднозначность выбора.

Путь Parquet проверен на локально преобразованных копиях **всех** предоставленных Markdown-строк. Оригинальные Parquet-файлы организаторов отсутствовали. Результаты преобразованного набора совпали с Markdown. pandas не требуется: на этой машине его нативная библиотека была заблокирована Windows Application Control, поэтому загрузчик использует Arrow напрямую. Политика Windows не изменялась.

## Просмотр и локальное размещение

Проверенный способ просмотра — локальный сервер по команде ниже. Введите полный `gid`, например `100000003684369100`, нажмите «Найти» или Enter. Карточка показывает входящие и исходящие связи, суммы, количество переводов и правило роли. Есть поиск изолированных узлов, раскраска по ролям/кластерам, фильтр кластеров, перемещение и масштабирование. В разделе «Правило роли и расчёт приоритета» видны условия и пять вкладов в приоритет.

Для localhost запускается сервер, корнем которого служит **только каталог результатов**:

```powershell
python -m http.server 8765 --bind 127.0.0.1 --directory out
```

Адрес: [http://127.0.0.1:8765/](http://127.0.0.1:8765/). Остановка — Ctrl+C. Для результатов Parquet замените `out` на `parquet_out`. Интернет для такого просмотра не нужен. Альтернатива — открыть самодостаточный `out/index.html` напрямую; запуск через `file://` в этой сессии не проверен из-за ограничения браузерной автоматизации. Публичное размещение на GitHub Pages или Vercel не выполнялось.

В браузере 18-значные `gid` хранятся строками, чтобы JavaScript не округлял идентификаторы. При открытии CSV в Excel задавайте для `gid` текстовый формат: автоматическое преобразование может потерять цифры. Положение точек на экране помогает читать граф и не является аналитической метрикой.

## Данные и валидация

Контракт фиксирован для этого кейса: **1–31 июля 2026 года**, внутрибанковские переводы от **5 000 KZT**, обход исходящих связей от seed на четыре шага.

| Таблица | Допустимые исходные поля |
|---|---|
| `nodes` | `gid`, `depth`, `is_seed` |
| `edges` | `src`, `dst`, `sum_kzt`, `n_tx`, `depth` |
| `transactions` | `src`, `dst`, `date`, `sum_kzt` |

`load_tables`, `normalize_tables` и `validate_inputs` в `pipeline.py` проверяют точный состав колонок, int64-идентификаторы без преобразования из float, глубины и согласованность seed с глубиной 0. Для Parquet проверяются типы: идентификаторы и `n_tx` — int64, глубины — знаковые целые, суммы — float64, seed — bool, дата — календарная дата либо timestamp без часового пояса ровно в полночь.

Проверяются уникальность узлов и направленных пар, наличие концов каждого ребра в `nodes`, конечность положительных сумм, положительное количество переводов, даты июля и порог 5 000 KZT для транзакций. Агрегаты транзакций обязаны совпасть с ребрами по парам, количеству и суммам. Суммы сравниваются через `math.isclose` с `rel_tol=1e-10`, `abs_tol=1e-6`. Чрезмерные значения, способные переполнить расчет, отвергаются. Неизвестные клиентские колонки не принимаются.

В наборе **81 seed**: **19** без ребер и еще **12** только с входящими. Все сохранены в итогах. Оборот — **365 890 012.01 KZT**; максимальная разница суммы транзакций и ребра — `5.820766091346741e-11 KZT`. Указанные в описании 16 слабосвязных компонент содержат ребра; вместе с 19 изолированными seed полный граф имеет **35 компонент**.

## Метрики и точные правила ролей

`in_deg/out_deg` — число разных плательщиков/получателей; `in_tx/out_tx` — количество отдельных переводов; `in_kzt/out_kzt` — наблюдаемые суммы. `pass_through = out_kzt / in_kzt`, если входящая сумма положительна, иначе значение отсутствует. Эта доля не является полным балансом счета.

PageRank следует направлению «плательщик → получатель» с весом `sum_kzt`, затуханием **0.85**, пределом **200 итераций** и остановкой при сумме абсолютных изменений меньше `1e-12`. Betweenness рассчитывается методом Brandes с Dijkstra по направленному графу: длина ребра **`1 / log(1 + sum_kzt)`**, нормировка **`(N-1)*(N-2)`**. Крупный перевод получает меньшую условную длину; это модель структурной связи, не время прохождения денег.

Квантили вычисляются заново. В отсортированных `N` значениях позиция равна `(N-1)*q`; между соседями применяется линейная интерполяция. Точные пороги текущего `analysis_summary.json`:

| Порог | Значение |
|---|---:|
| P75(`in_deg`) | `1.0` |
| P90(`in_deg`) | `2.0` |
| P75(`out_deg`) | `1.0` |
| P90(`out_deg`) | `3.0` |
| P75 положительных betweenness | `9.738917745675346e-05` |
| P75 PageRank по всем узлам | `0.00046046328200250765` |

Для betweenness берутся положительные значения, поскольку множество нулей скрывает различия между посредниками. Нижняя граница степени — `max(1, квантиль)`, чтобы нулевой квантиль сам по себе не создавал сигнал. Константы доли пересылки и силы правил — заданные эвристики; они не подгоняются под конкретные `gid`.

Правила в `build_features` (`pipeline.py:589`) применяются **сверху вниз; первое совпадение завершает выбор**:

| № | Роль и точное условие | `role_score` |
|---|---|---:|
| 1 | `terminal`: `out_deg == 0` и `depth == 4` | `0.45` |
| 2 | `terminal`: `out_deg == 0` и `in_deg > 0`; после правила 1 глубина меньше 4 | `0.82` для не-seed; `0.50` для seed |
| 3 | `coordinator`: `in_deg >= max(1, P75(in_deg))`, `out_deg >= max(1, P75(out_deg))`, `betweenness > 0` и `betweenness >= P75(положительных betweenness)` | `0.76` |
| 4 | `consolidator`, не-seed: `in_deg >= max(1, P90(in_deg))`, доля пересылки существует и `< 0.50` | `0.72` |
| 5 | `consolidator`, seed: `in_deg >= max(1, P90(in_deg))` и `pagerank >= P75(pagerank)` | `0.62` |
| 6 | `transit`, не-seed: `in_deg >= max(1, P75(in_deg))`, доля пересылки существует и `0.70 <= pass_through <= 1.30` | `0.68` |
| 7 | `distributor`: `out_deg >= max(1, P90(out_deg))` | `0.66` |
| 8 | `peripheral`: ни одно предыдущее правило не сработало | `0.50` |

У правила 5 наличие исходящего ребра следует из порядка терминальных правил. Для seed накопление — более слабая гипотеза по степени и PageRank, поскольку доля пересылки искажена неполными входящими. Изолированный seed получает `peripheral`: наблюдений о потоках нет.

`evidence` содержит реальные числа и оговорки seed/глубины 4, максимум **200 символов**. Повторные переводы учитываются в `in_tx/out_tx` и объяснении; самостоятельного порога по их количеству для назначения роли нет. В правилах счетный сигнал задается числом разных контрагентов, денежный — долей пересылки и взвешенными метриками. Множество операций одного плательщика не приравнивается к множеству независимых плательщиков.

Распределение: `consolidator` — **88**, `transit` — **92**, `distributor` — **85**, `terminal` — **1 535**, `coordinator` — **147**, `peripheral` — **301**. Среди терминальных **444** имеют score `0.45` из-за границы четвертого шага. Все значения `role_score` — фиксированная сила правила, **не калиброванная вероятность**.

## Четыре ловушки данных

| Ловушка | Обработка | Указатель на код |
|---|---|---|
| Обрыв на четвертом шаге | `depth == 4` и нулевой исходящий degree дают `truncated_by_depth`, score `0.45` и явную оговорку. Ранние не-seed с входом и без выхода получают `0.82`; вывод ограничен выгрузкой. | `build_features`, `pipeline.py:547`, `pipeline.py:589` |
| Заниженные входящие seed | Ratio-правила исключают seed. Отдельное правило использует degree и PageRank. Все 81 evidence содержат оговорку о входящих. | `build_features`, `pipeline.py:600`, `pipeline.py:614` |
| Сумма и число операций | Отдельно считаются суммы, количество переводов и контрагентов; degree и суммы участвуют в роли, количество переводов — в evidence. | `build_features`, `pipeline.py:537` |
| Направление и вес | PageRank, betweenness и потоки сохраняют направление. Ненаправленная проекция применяется к Louvain и близости к seed. | `pagerank`, `pipeline.py:338`; `weighted_directed_betweenness`, `pipeline.py:358`; `louvain_partition`, `pipeline.py:425` |

## Кластеры и гипотезы

Многоуровневый **Louvain** работает на ненаправленной проекции с весом `sum_kzt`, разрешением **1** и генератором перестановок **seed 41**; встречные веса суммируются. Пределы реализации — **20 уровней**, **100 проходов на уровень**. Сообщество группирует связанные потоки, его состав сам по себе не доказывает единую организацию. Изолированные узлы остаются отдельными сообществами. Номера упорядочены по убыванию размера, затем по минимальному `gid`; при изменении данных номера могут измениться.

`make_cluster_rows` (`pipeline.py:701`) считает `sum_kzt_internal` по исходным **направленным** ребрам с обоими концами внутри сообщества. `top_gids` содержит до пяти участников по полному приоритету; при равенстве выбирается меньший `gid`. Число структурных сигналов — сумма числа `consolidator`, `coordinator`, `distributor` и `transit`.

Текст `hypothesis` выбирается по составу в таком порядке:

1. Есть seed и consolidator/coordinator — связь seed с накоплением или координацией.
2. Есть consolidator/coordinator — накопление или координация в наблюдаемой сети.
3. Есть distributor — веерное распределение.
4. Есть transit — пересылка средств.
5. Иначе — фрагмент без выраженного сигнала.

Каждый текст начинается с `Hypothesis`, содержит **число seed / число узлов**, число структурных сигналов и измеренный внутренний оборот, завершаясь предложением проверить транзакции. Оборот в тексте округлен до целого KZT; числовая колонка сохраняет расчетное значение. Глобальная оптимальность разбиения не заявляется.

## Формула приоритета

`build_features` (`pipeline.py:555`) использует процентильные ранги:

```text
priority_score = 0.30 * rank(pagerank)
               + 0.25 * rank(in_deg)
               + 0.25 * rank(betweenness)
               + 0.10 * rank(out_deg)
               + 0.10 * rank(seed_reach)
```

Для `N > 1` ранг — средняя позиция среди равных значений, считая с нуля, деленная на `N-1`. Равные значения получают одинаковый ранг; для единственного узла он равен `0.5`. Ранги лежат в `[0,1]`, веса суммируются в 1. Формула объединяет значимость получателя, число связей, посредничество и близость к seed без сложения исходных величин разных шкал.

`seed_reach` — число разных seed на расстоянии **не более двух шагов в ненаправленном соседстве**. Seed учитывает себя на расстоянии 0 (`seed_reach_within_two_hops`, `pipeline.py:403`). Это близость в графе, не число направленных денежных маршрутов.

Сортировка — по полному приоритету по убыванию, затем числовому `gid` по возрастанию. CSV хранит шесть знаков после запятой: визуально одинаковые оценки могут иметь разные полные значения. `why` включает приоритет и объяснение роли. `role_score` и номер кластера в формулу не входят.

## Самостоятельная проверка

```powershell
python verify.py
python verify.py --gid 100000003684369100
python verify.py --report .audit/verification.json
```

`verify.py` заново читает данные и независимо рассчитывает агрегаты, направленные метрики, правила и приоритет, не импортируя функции пайплайна. Проверяются точные CSV-схемы, все узлы, числовые утверждения evidence, оговорки, связность/состав кластеров, суммы, рейтинг и данные внутри HTML, включая идентификаторы и прямые ребра. Код **0** и `PASS` означают успех; при расхождении выводятся `FAIL` и ненулевой код. Проверка связности и учета кластеров не доказывает глобальный максимум Louvain.

`--gid` печатает сырые входящие/исходящие ребра и сработавшее правило. `--report` сохраняет JSON только внутри проектного `.audit/`; он тоже содержит графовые данные. Для Parquet:

```powershell
.venv\Scripts\python.exe verify.py --data parquet_data --out parquet_out
```

Проверки ошибочных входов, файловых перенаправлений и поврежденных выгрузок:

```powershell
.\.venv\Scripts\python.exe -B -m unittest discover -s tests -v
```

В аудите с установленным PyArrow выполнены **26 тестов: 25 прошли, 1 пропущен** из-за отсутствующей Windows-привилегии создания настоящего symlink (`WinError 1314`). Проверки настоящих junction и hard link прошли. Можно использовать обычный `python -B -m unittest discover -s tests -v`; без PyArrow дополнительно пропускается проверка типов Parquet. Браузерный поиск и изолированные узлы проверяются отдельно от встроенного JSON; сценарий — в HOWTO.

## Измеренная воспроизводимость

Замеры на этой машине с Python **3.14.0**, включая запуск процесса:

| Проверка | Время | Результат |
|---|---:|---|
| Копия исходников и данных в новый каталог без `out/`, Markdown | **0.9094104 с** | Все результаты созданы, независимая проверка прошла |
| Полный локально преобразованный набор Parquet, первый запуск | **17.1664882 с** | Успех |
| Тот же Parquet-набор, повторный запуск | **1.0734238 с** | Успех |

Установка зависимостей не входит в лимит пересчета. Все замеры укладываются в пять минут; скорость другого компьютера может отличаться. Проверялась новая файловая копия, а не Git checkout: в рабочем каталоге нет `.git`. Правила, три CSV и HTML сохранили исходные результаты; три CSV и HTML сверены побайтово. Время в `analysis_summary.json` измеряется внутри процесса и меняется между запусками.

## Схема решения

```text
Markdown / Parquet
        |
        v
Схема, типы, даты, суммы, узлы, сверка transactions <-> edges
        |
        v
Направленный взвешенный граф, включая изолированные nodes
        |
        +--> degree, KZT, количество операций, PageRank, betweenness
        |                         |
        |                         +--> правила --> роли + evidence
        |                         +--> ранги --> приоритет
        |
        +--> ненаправленная проекция --> Louvain --> кластеры + гипотезы
                                      |
                                      v
                  3 CSV + контрольный JSON + локальный HTML
```

## Ограничения и масштабирование

Отсутствуют межбанковские переводы, входящие из-за границ выборки, суммы ниже 5 000 KZT и продолжение обхода после глубины 4. Полный баланс, последовательность прохождения конкретных денег и дробление ниже порога установить нельзя. `terminal` до четвертого шага означает отсутствие захваченного исходящего потока в этой выгрузке.

Размеченных истинных ролей нет: accuracy, полнота обнаружения и вероятность роли не оценены. Фиксированные scores не калиброваны. Квантили степени на разреженном графе могут быть малы; порядок правил может скрывать вторую подходящую роль. Месячная доля пересылки не подтверждает быстрый транзит. Seed-правило консолидации — более слабый структурный признак, без утверждения об остатках денег.

Даты и минимальная сумма привязаны к июлю 2026 и этому контракту; для другого периода требуется осознанная настройка кода. Временные паттерны, циклы, удаление узлов и внешние сведения сейчас не рассчитываются. Номера кластеров нельзя сравнивать между снимками без сопоставления. Весь граф хранится в памяти и HTML. Многопользовательский публичный сервис с разграничением доступа не реализован.

При росте до **примерно 1 млн узлов** точная betweenness по всем источникам станет узким местом. Потребуются графовая БД или другое хранилище с индексами, разбиение по компонентам/периодам, приближенная betweenness по выборке источников и PageRank на подходящем вычислительном движке. Обновления следует считать для затронутых областей; глобальные квантили — версионировать по снимкам. Интерфейс должен запрашивать ограниченное соседство и сводки сообществ вместо полного графа в HTML. Эти изменения описаны как план, не заявлены реализованными.

## Безопасность и публикация

Организаторы разрешили данные **исключительно для хакатона**. Рекомендуется приватный репозиторий и отдельная передача входов/результатов команде. В этой папке нет `.git`, remote и доступной истории, поэтому внешняя видимость и наличие опубликованных копий не установлены. Публичная публикация данных не подтверждена.

`.gitignore` исключает `data/`, `out/`, `parquet_data/`, `parquet_out/`, `.audit/`, `.env`, `.env.*`, окружение и ключевые файлы; для шаблона `.env.example` предусмотрено исключение. Он предотвращает случайное добавление новых файлов, но не удаляет историю другого репозитория. **`index.html` содержит весь граф**: его публикация раскрывает набор переводов. Перед публичным размещением требуется отдельно согласовать использование с организаторами.

Секреты в проверенных исходниках и данных не найдены. Пайплайн не требует API-ключей, GPU или внешних сервисов, не использует `eval`, pickle либо команды из входных строк. JSON экранирует HTML-разделители, интерфейс выводит текст через `textContent`.

`--out` означает явно выбранный пользователем каталог. Существующие symlink, junction, reparse point, специальные файлы и многократно связанные файлы результатов отвергаются. Каждый файл заменяется атомарно из временного файла в том же каталоге. Это не транзакция для пяти файлов одновременно и не защита от владельца, враждебно меняющего каталог во время записи; используйте доверенную локальную папку.

Подробные находки, исправления и ограничения: [SECURITY.md](SECURITY.md), [AUDIT_REPORT.md](AUDIT_REPORT.md).

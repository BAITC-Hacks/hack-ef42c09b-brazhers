# Optional: these dependencies are required only when reading Parquet inputs.
# The included Markdown workflow runs without installing packages.
# Parquet mode requires Python >=3.10; audited 2026-09-23.
pyarrow==25.0.1
